extends layout

block content
  h1= title
  p Welcome to #{title}
  form(method="get")
    input(type="text" name="search" value=searched || '')
    input(type="submit" value="Szukaj")
  each item in data 
    article 
      h1=item.title
      p=item.created
      p=item.description